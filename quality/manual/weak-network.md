# NETWORK-001：弱網、離線與重連

## 前置條件

- 兩支已配對真實 iPhone，使用可回收測試內容。
- 明確記錄本版本哪些內容已承諾持久 Outbox；尚未實作的能力標記 `NOT_APPLICABLE`，不得宣稱通過。

## 步驟與預期

1. A、B 同時離線各送出三則正式文字訊息；輸入框只在 Outbox 保存成功後清空，每則立即顯示但不得標示為已同步，強制結束再重開後待送資料仍存在且原輸入順序不變，不得出現輸入清空但沒有泡泡的情況。
2. A 由 Wi-Fi 切換行動網路，B 做短暫 Wi-Fi 中斷；恢復後自動或由明確重試送達，另一支正在前景的手機須即時顯示，不得要求重啟 App。
3. 同一 stable client ID 重試不產生重複；多筆內容維持可預期 FIFO／server order。
4. App 在背景、前景恢復與離線冷啟動時先由唯讀快照回到原 relationship，不等待遠端逾時；不得並行重送同一項目，快照也不得讓已失效的遠端寫入繞過 RLS。
5. 已登入且已有 relationship 快照時，離線冷啟動仍顯示「今天／對話／我們」主畫面及離線提示；不得自動開啟帳號設定或其他 modal。
6. 舊 relationship、closing／archived relationship 的待送內容不會誤送到目前伴侶。
7. 保持離線直到有限嘗試停止；UI 保留傳送失敗與明確重試，不無限輪詢。恢復網路後點重試只送達一次。
8. UI 清楚區分傳送中、失敗、已同步；本機待送內容不得標示為已備份。
9. 兩支手機至少先在線成功載入一次對話；離線強制結束再開啟時仍顯示最近同步的聊天內容與待送訊息，不得顯示成全新空聊天室。恢復網路後以遠端內容校正且不重複。
10. 兩支手機至少先在線成功載入一次「今天」；離線強制結束再開啟時，最新 Moment、照片、雙方稱呼及未過期狀態先由唯讀快照顯示，不得長時間只顯示載入畫面。已過期狀態不得因離線快照重新出現；恢復網路後不用重啟 App 即以遠端 RLS 結果校正。
11. 成功登出後，前一個 user 的 relationship、聊天快照與待送內容不會顯示於下一個登入身分；遠端確認 relationship 已失效或改變時亦不保留舊關係聊天資料。

## W10 加驗（部分完成）

1. A 離線後依序送出文字 T1、照片 P1、文字 T2；三項都在 Outbox 保存成功後才清除 composer，並以待送狀態按原順序顯示。
2. 保持離線強制結束 App 再重開；T1、P1、T2 與 P1 的本機照片資料仍存在且順序不變，不得把任一項標成已同步。
3. 恢復網路；B 最終只收到一次 T1→P1→T2，P1 可開啟，前一項失敗或重試時後一項不得超車。A 的 Outbox 成功清空後，本機待送照片檔也應清除。
4. 在 P1 上傳完成但回應遺失的條件重試；相同 stable client ID 不得建立第二則訊息或第二份 Storage object。

## W11 加驗（部分完成）

1. A 離線修改約定開始時間，再強制結束並重開；待送操作與樂觀的新時間保留，恢復網路後 B 收斂至新時間且兩台只出現一筆永久調整紀錄。
2. 模擬伺服器已接受時間調整但 acknowledgement 遺失；相同 operation UUID 重送不得重複紀錄，也不得覆蓋 B 在其後完成的更新。
3. A 離線依序調整另一筆約定時間再取消；恢復網路後仍依 FIFO 收斂，兩筆紀錄各出現一次，取消後不得被前後待送編輯復活。
4. A 離線建立含提醒的約定；同步前不得排程，恢復網路並同步後只排一次。force-quit 重開不得增加第二個通知，之後取消則必須移除待送提醒。

## 通過證據

記錄每輪網路轉換、force-quit 時點、送達次數、順序與 Outbox 最終狀態；W10 另記錄 T1／P1／T2 的 stable client ID 前 8 碼與照片暫存檔清理結果；W11 另記錄 appointment、operation 與永久紀錄 ID 前 8 碼。漏送、重複、錯序、無限重試、殘留照片、重複／遺失重大紀錄或送錯 relationship 都阻擋發布。

2026-08-13 W9 關閉複驗：兩支真實 iPhone 已確認三分頁離線冷啟動保留歷史、前景 Moment／文字訊息即時顯示，以及離線期間新增內容在接收方恢復網路後自動補齊且不需重啟 App。正式文字聊天未觀察到遺失、重複或錯序；G8／W9 通過。W10 的聊天照片、訊息 Emoji 回應與收藏為 Moment 不納入本次結果，仍待獨立執行上述加驗。

2026-08-13 W10 實機複驗：兩支 iOS 26.6 真實 iPhone 已完成步驟 1–3。A 離線依序送出 T1→P1→T2，force-quit 後文字、照片與順序均保留；恢復網路後 B 僅收到一次且沒有超車或重複，照片可開啟。stable client ID 前 8 碼與照片暫存檔清理路徑未記錄；步驟 4 的「Storage 上傳成功但 ack 遺失」故障注入尚未執行，因此本清單仍為部分完成。

2026-08-14 W11 首輪實機驗收：共同約定通用提醒與點擊來源跳轉通過。A 離線修改開始時間後強制結束並於離線狀態重開，relationship／Today 快照正常恢復，但共同約定顯示為空並提示無法更新；根因是 operation Outbox 缺少已同步約定的本機顯示基底。修正加入 user＋relationship scoped 的最近 200 筆已同步約定快照，冷啟動先恢復快照再疊加待送操作，遠端成功後仍由 RLS 結果校正；快照 scope／容量與離線重啟待送修改兩個 focused runtime regressions 已通過。第二輪真機尚待安裝修正版後由步驟 1 重新執行，第三、第四輪尚未開始。

2026-08-17 W11 第二次實機驗收：安裝快照修正版並在線建立顯示基底後，A 離線改期、強制結束及離線重開時約定與待送提示均存在，但開始時間仍顯示舊值，直到恢復網路才由兩台收斂至新值。這確認 operation Outbox 未遺失，但啟動流程把本機 operation 疊加排在遠端 refresh／Realtime 之後。修正改為載入快照後、任何遠端等待前立即依 FIFO 套用待送修改／取消；更新後的 focused regression 會懸停遠端讀取，並確認尚未呼叫 delivery 時已顯示修改後時間與本機待送提示。第二輪仍待修正版第三次實機複驗，第三、第四輪尚未開始。

2026-08-17 W11 第二輪第三次實機驗收：兩支真實 iPhone 已確認 A 離線改期、強制結束及離線重開後，修改後時間與待同步提示立即保留；恢復網路後兩台收斂，第二輪核心流程通過。驗收另發現全域離線提示會壓到共同約定詳情／編輯頁左上角導覽控制；client 修正把提示改為主分頁上方的一般版面列，讓所有約定 sheet 完整覆蓋它。focused UI regression 已走完「我們 → 共同日程 → 約定詳情 → 編輯」，確認主畫面提示存在、編輯頁的底層提示不可互動且取消按鈕可操作；仍待把此視覺修正版安裝到真機確認。第三、第四輪尚未開始。
